<ul>
  <% urls.each do |url| %>
    <li class='article'>
      <h3><%= url.title %></h3>
      <p class='description'><%= url.description %></p>
      <%= link_to "http://shre.xyz/#{url.slug}", "http://shre.xyz/#{url.slug}", class: 'link-article' %>
    </li>
  <% end %>
</ul>
